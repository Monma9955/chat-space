.chat-side
  .side-header
    .side-header__box
      .side-header__box__user-name
        = current_user.name
      .side-header__box__menu
        .side-header__box__menu__new-group
          = link_to new_group_path(current_user), class: "menu__btn ffff" do
            = icon('fas', 'edit')
        .side-header__box__menu__edit-user
          = link_to edit_user_path(current_user), class: "menu__btn ffff" do
            = icon('fas', 'cog')
  .groups
    .group
      .group__group-name
        Test1
      .group__latest-message
        まだメッセージはありません
    .group
      .group__group-name
        Test2
      .group__latest-message
        まだメッセージはありません